spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:scm_db}?serverTimezone=UTC
    username: ${MYSQL_USER:scm}
    password: ${MYSQL_PASSWORD}
  thymeleaf:
    cache: true

sentinel:
  demo:
    show-credentials: ${SENTINEL_DEMO_SHOW_CREDENTIALS:true}
